# Edge Server Configuration
CHRONOS_EDGE_PORT=5000

# Deployment Configuration
EDGE_SERVER_HOST=pleasechangeme
# Use a commit hash or tag instead of a branch name to avoid git divergence issues
# Example: GIT_REF=5d6aabc7b43a9c668d09618105addad6b13af1cf
GIT_REF=main

# Service Configuration
PI_USER=pi
INSTALL_DIR=/opt/chronos
