[project]
name = "edge-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "pyserial",
    "pymodbus",
    "python-dotenv",
    "pydantic",
    "pydantic-settings",
    "loguru",
    "pytest",
    "httpx",
    "ipython>=8.30.0",
    "pdbpp>=0.10.3",
]
